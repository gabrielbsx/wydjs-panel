<script>
    pages.push('donate-packages');
    pages.push('donate-items');

    async function getdonatepackages() {

    }

    async function updateApi(url, id) {
        console.log(id);
        $('.update-title').append(url.replaceAll('-', ' ').replace('/', ''));
        $('.update').toggle();
    }
    
    async function deleteApi(url, id) {
        console.log(id);
    }

    $(document).ready(async () => {
        const translate = {
            'id': 'Identificador',
            'name': 'Nome',
            'player': 'Jogador',
            'percent': 'Porcentagem',
            'donate': 'Donate',
            'value': 'Valor',
            'createdAt': 'Criado em',
            'updatedAt': 'Atualizado em',
        };

        await getApi('/donate-packages', '.list-donate-packages');

    });
</script>